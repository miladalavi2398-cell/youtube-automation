name: My Video Maker

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "🎬 Starting video creation..."
    
    - name: Install tools
      run: sudo apt update && sudo apt install -y ffmpeg
      
    - name: Make video
      run: |
        ffmpeg -f lavfi -i color=c=green:s=320x240:d=2 -c:v libx264 output.mp4
        ls -lh output.mp4
        
    - run: echo "✅ Done! Check Artifacts below."
